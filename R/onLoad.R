

.onLoad <- function(libname, pkgname) {
  
  options(
    
    # unicode support 
    # MUST as of macOS, R 4.5.1
    bitmapType = 'cairo' 
    # must ship with package to alter user's options
    
  )  
  
}

